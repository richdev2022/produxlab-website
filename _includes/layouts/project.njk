<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">

    {# SEO Meta Tags (adapt for portfolio if needed, often similar to blog) #}
    <title>{{ seo_title or title }} | ProdUXLab Portfolio</title>
    <meta name="description" content="{{ seo_description or description or body | striptags | truncate(160) }}">
    <meta name="keywords" content="{{ seo_keywords or tech_stack | join(', ') }}">

    {# Open Graph / Social Media Meta #}
    <meta property="og:title" content="{{ seo_title or title }}">
    <meta property="og:description" content="{{ seo_description or description or body | striptags | truncate(160) }}">
    {% if thumbnail %}
        <meta property="og:image" content="{{ thumbnail | url }}">
    {% endif %}
    <meta property="og:type" content="website"> {# Or 'article' if it's a deep case study #}
    <meta property="og:url" content="{{ canonicalUrl or page.url | url }}">

    <link rel="icon" href="/img/favicon.svg">

</head>
<body>
    <header>
        <h1>ProdUXLab</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/blog/">Blog</a>
            <a href="/portfolio/">Portfolio</a>
        </nav>
    </header>

    <main>
        <article>
            <h1>{{ title }}</h1>
            <p>Client: {{ category }}</p>
            <p>Completed on: {{ date | htmlDateString }}</p>
            {% if thumbnail %}
                <img src="{{ thumbnail | url }}" alt="{{ title }}" style="max-width: 100%; height: auto;">
            {% endif %}
            <div class="project-overview">
                {{ body | safe }} {# This renders the Markdown content #}
            </div>
            {% if tech_stack %}
                <p>Technologies Used:
                    {% for tech in tech_stack %}
                        <span>{{ tech }}</span>{% if not loop.last %}, {% endif %}
                    {% endfor %}
                </p>
            {% endif %}
            {% if live_url %}
                <p><a href="{{ live_url }}" target="_blank" rel="noopener noreferrer">View Live Project</a></p>
            {% endif %}
            {% if github_url %}
                <p><a href="{{ github_url }}" target="_blank" rel="noopener noreferrer">View on GitHub</a></p>
            {% endif %}
        </article>
        <p><a href="/portfolio/">← Back to Portfolio</a></p>
    </main>

    <footer>
        <p>&copy; {{ "now" | date("yyyy") }} ProdUXLab. All rights reserved.</p>
        <nav>
            <a href="/Terms.html">Terms</a> |
            <a href="/Privacy.html">Privacy</a> |
            <a href="/Refund.html">Refund</a>
        </nav>
    </footer>

    <script src="/javascript.js"></script>
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</body>
</html>